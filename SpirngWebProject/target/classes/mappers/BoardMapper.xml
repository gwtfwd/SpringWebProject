<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="kr.green.springwebproject.dao.BoardMapper">
  
    <select id="getBoard" resultType="kr.green.springwebproject.dao.Board">
		select * from board 
	</select>
	
	<select id="getBoardByNumber" resultType="kr.green.springwebproject.dao.Board">
		select * from board where number=#{number} 
	</select>
	
	<update id="modifyBoard">
		update board set title=#{board.title}, author=#{board.author}, contents=#{board.contents} where number=#{board.number}
	</update>
	
	<insert id="insertBoard">
		insert into board (title, contents, author) values (#{board.title}, #{board.contents},#{board.author})
	</insert>

</mapper>